buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.2'  // Use latest stable version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}